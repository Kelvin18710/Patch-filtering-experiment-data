--- ./origin/Lang_26\FastDateFormat.java
+++ ./source/Lang_26\Lang_26_simfix_patches+53\53.java
@@ -817,9 +817,9 @@
      * @return the formatted string
      */
     public String format(Date date) {
-        Calendar c = new GregorianCalendar(mTimeZone);
-        c.setTime(date);
-        return applyRules(c, new StringBuffer(mMaxLengthEstimate)).toString();
+Calendar c=new GregorianCalendar(mTimeZone);
+c.setTime(date);
+return new StringBuilder(mMaxLengthEstimate+mMaxLengthEstimate-mMaxLengthEstimate+mStandard.length()+1).append(mStandard.substring(0,start)).append(overlay).append(str.substring(end)).toString();
     }

     /**